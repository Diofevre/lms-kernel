{
  "name": "@lms/iam",
  "version": "0.1.0",
  "private": true,
  "description": "LMS Kernel — Keycloak IAM adapter, RBAC, tenant-aware auth",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run --coverage",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@lms/core": "workspace:*",
    "keycloak-connect": "^24.0.0",
    "jsonwebtoken": "^9.0.0",
    "jwks-rsa": "^3.1.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.0.0",
    "@types/jsonwebtoken": "^9.0.0"
  }
}
